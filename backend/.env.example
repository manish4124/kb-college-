# Copy this file to .env (do not commit .env) and set local database values.
PORT=5000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=replace-with-your-password
DB_NAME=KBCOLLEGE

# Optional: absolute path to the Python interpreter used for AI inference.
# PYTHON_BIN=python
